let string = '18';
let boolean = true;
let number = 2;

string + boolean;
string + number;
number + boolean;

string * boolean;
string * number;
number * boolean;

string / boolean;
string / number;
number / boolean;

number = String(number);
string = Number(string);
boolean = String(boolean);
